# .NET / build output
bin/
obj/
*.user
*.suo

# Rider / VS
.idea/
.vs/
*.DotSettings.user

# Velopack / publish artifacts (when added later)
publish/
Releases/

# Generated logo candidates (large; the chosen one lives in Assets/). Keep the
# prompt recipe (art/logo-prompts.json) tracked for reproducibility.
art/*.png

# OS cruft
Thumbs.db
.DS_Store
